accountant:
  email: accountant@first.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

manager:
  email: manager@first.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

eavesdropper:
  email: eavesdropper@second.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
